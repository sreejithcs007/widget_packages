import 'package:flutter/material.dart';

class AmosMain extends StatelessWidget {
  const AmosMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
