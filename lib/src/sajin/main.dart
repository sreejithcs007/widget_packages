import 'package:flutter/material.dart';

class SajinMain extends StatelessWidget {
  const SajinMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
